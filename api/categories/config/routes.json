{
  "routes": [
    {
      "method": "GET",
      "path": "/categories",
      "handler": "categories.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/categories",
      "handler": "categories.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/categories",
      "handler": "categories.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
